# PRD — 크리에이터 매칭 추천 프로토타입

작성: 이용환 · 2026.08

---

## 1. 이 프로토타입이 해결하려는 문제

광고주가 인플루언서 캠페인을 준비할 때 실제로 막히는 지점은 "누가 있는지 모르는 것"이 아니라 **"이 예산으로 누구를 골라야 하는지 판단할 근거가 없는 것"** 이다.

목록을 팔로워 순으로 정렬해 보여주는 것만으로는 이 문제가 풀리지 않는다. 팔로워가 많다고 성과가 좋은 것이 아니고, 광고주는 그 사실을 알지만 대안 기준을 스스로 세우기 어렵다. 그래서 이 프로토타입의 목표는 **입력 조건에 맞는 후보를 성과 기반으로 줄 세우고, 왜 그 순서인지 설명하는 것**이다.

핵심 사용자는 **캠페인 집행을 결정하는 광고주·대행사 담당자**로 정의했다. 이 사람은 데이터를 매일 다루지만 데이터 분석가는 아니며, 고른 결과를 내부에 설명해야 하는 위치에 있다. 따라서 결과는 "점수"만이 아니라 **근거가 함께** 제시되어야 한다.

---

## 2. 핵심 기능 정의

| 기능 | 내용 | 구분 |
|---|---|---|
| 입력 폼 | 예산(원) · 카테고리(다중) · 팔로워 규모(3단) | 필수 |
| 추천 결과 | 조건 충족 후보를 스코어 순 카드로 노출 | 필수 |
| 예외 처리 | 후보 0명 시 조건 완화안 제시 (계산된 대안) | 필수 |
| 추천 근거 | 도달·신뢰·경험 3축 막대 + 근거 태그 | 가산점 |
| 정렬/필터 | 점수·**예산 효율**·참여율·조회수·집행건수·평점·단가 7종 재정렬 | 가산점 |
| 예산 효율 | 도달 1인당 비용(CPR) 표시 + 전체 중앙값 대비 위치 | 가산점 |
| 위험 신호 | 팔로워 대비 조회수가 같은 플랫폼 하위 10%인 경우 주의 태그 | 가산점 |
| 조합 제안 | 예산 내 다인 구성안 (안정형 / 테스트형) | 선택(별도 탭) |

---

## 3. 추천 로직의 기준

### 3.1 필터 (하드 조건)

```
카테고리 ∈ 선택한 카테고리 (미선택 시 전체)
팔로워    ∈ 선택한 규모 구간
단가      ≤ 총예산
```

### 3.2 스코어 (0~100)

```
score = 실질 도달 45% + 광고주 평점 30% + 집행 경험 25%
```

각 지표는 **선택된 팔로워 구간 안에서의 백분위**로 환산한다. 절대값이 아니라 상대 순위를 쓰는 이유는 두 가지다. ①지표별 단위가 달라(조회수는 만 단위, 참여율은 한 자릿수 %) 직접 합산할 수 없고, ②같은 구간 안에서만 비교해야 규모 편향이 상쇄되기 때문이다.

**가중치 근거**

- **실질 도달 45%** — 광고 성과에 가장 직접적인 축. 단, 조회수와 참여율을 따로 넣지 않고 `평균 조회수 × 참여율` 하나로 합쳤다. 데이터를 보면 참여율은 p25 6.47 / 중앙값 7.10 / p75 7.90으로 **전 구간이 1.4%p 안에 몰려 있어** 단독 지표로는 변별력이 거의 없다. 반면 두 값을 곱하면 "실제로 반응한 사람 수"라는 의미 있는 단일 축이 된다.
- **광고주 평점 30%** — 재구매 의사에 가장 가까운 신뢰 지표. 다만 13.5%가 결측이라 단독 1위 가중치는 주지 않았다.
- **집행 경험 25%** — 협업 안정성의 대리 지표. 다만 이 축은 신인에게 구조적으로 불리하므로, 별도 노출 구조로 보완한다(3.4 참조).

**가중치 값 자체는 최적해가 아니다.** 45/30/25가 최선이라는 근거는 없다. 그래서 이 값이 결과를 얼마나 좌우하는지를 먼저 확인했다. 마이크로 밴드의 검증 후보 115명을 대상으로 가중치를 흔들어 순위 변화를 측정했다.

| 가중치 | TOP10 겹침 | TOP20 겹침 | 1위 |
|---|---|---|---|
| **45/30/25 (채택)** | 기준 | 기준 | 기준 |
| 40/35/25 | 10/10 | 19/20 | 동일 |
| 50/30/20 | 9/10 | 19/20 | 동일 |
| 33/34/33 (균등) | 9/10 | 17/20 | 동일 |
| 60/20/20 | 8/10 | 17/20 | 바뀜 |
| 25/30/45 | 7/10 | 12/20 | 바뀜 |

**±5 범위에서는 결과가 거의 움직이지 않는다.** 40/35/25는 TOP10이 완전히 동일하다. 즉 "왜 하필 45인가"는 이 데이터에서 실익이 없는 질문이다.

반면 **축을 제거하면 결과가 무너진다.**

| 제거한 축 | TOP10 겹침 |
|---|---|
| 도달 | 6/10 |
| **평점** | **4/10** |
| 경험 | 6/10 |

이 대비가 말하는 바는 분명하다. **중요한 결정은 "비율을 몇으로 할까"가 아니라 "어떤 축을 쓸까"였다.** 비율은 ±5 안에서 임의로 정해도 무방하지만, 축 하나를 빼면 시스템이 다른 결과를 낸다.

특히 **평점을 제거했을 때 가장 크게 흔들린다**는 점이 눈에 띈다. 가중치는 30%로 도달(45%)보다 낮은데 영향은 더 크다. 아래 상관 분석이 그 이유를 설명한다.

**세 축이 서로 겹치지 않는지 상관계수로 검증했다.** 가중 합산은 축끼리 같은 정보를 담고 있으면 그 정보에 이중으로 가중치를 주게 되므로, 축 선정 자체가 유효한지 확인이 필요했다.

| 축 조합 | 상관계수 | 해석 |
|---|---|---|
| 평점 ↔ 참여율 | -0.02 | 무관 |
| 평점 ↔ 집행 건수 | 0.04 | 무관 |
| 평점 ↔ 실질 도달 | 0.07 | 무관 |
| 평점 ↔ 단가 | 0.02 | 무관 |

**광고주 평점은 다른 어떤 지표로도 대체되지 않는 독립 신호였다.** 규모가 크다고, 비싸다고, 경험이 많다고 평점이 높아지지 않는다. 이것은 평점에 30%라는 큰 가중치를 주는 근거가 된다 — 이 축을 빼면 시스템이 **다른 데이터로는 알 수 없는 정보**를 통째로 잃는다.

앞의 민감도 분석에서 평점 제거 시 순위가 가장 크게 흔들린 이유가 이것이다. 도달과 경험은 서로 다른 축이지만 둘 다 "채널이 큰가"를 어느 정도 반영한다. 반면 평점은 그와 무관한 정보를 홀로 담고 있어, 빼는 순간 대체할 것이 없다.

반대로 **단가는 성과 지표와 강하게 붙어 있었다.**

| 축 조합 | 상관계수 | 해석 |
|---|---|---|
| 단가 ↔ 팔로워 | 0.74 | 단가는 사실상 채널 규모의 함수 |
| 단가 ↔ 실질 도달 | 0.71 | 위와 동일 |
| 단가 ↔ 참여율 | **-0.51** | **비쌀수록 참여율은 오히려 낮다** |

마지막 줄이 이 프로토타입의 존재 이유를 데이터로 뒷받침한다. 광고주가 예산을 크게 쓸수록 큰 채널로 가게 되는데, **큰 채널일수록 팬의 반응 밀도는 떨어진다.** "비싼 채널 = 좋은 선택"이 성립하지 않으므로, 단가는 점수 축이 아니라 필터 조건과 효율 지표(3.6)로만 사용한다.

### 3.3 팔로워 구간 정의

과제에 기준이 명시돼 있지 않아 **세 가지 안을 데이터에 직접 대보고** 결정했다.

| 안 | 분포 | 판정 |
|---|---|---|
| 1만 / 1~10만 / 10만~ | 44 / 129 / 27 | **채택** |
| 5만 / 5~10만 / 10만~ | 122 / 51 / 27 | 5만 경계에 데이터적 근거 없음 |
| 10만 / 10~30만 / 30만~ | 173 / 14 / 13 | 상위 밴드에서 카테고리별 후보 0명 발생 |

채택안의 근거: 1만은 **p25(10,565)** 와 일치하고, 10만은 상위 14% 경계에 해당한다. 그리고 세 밴드 모두에서 **10개 카테고리 각각 최소 1명 이상**이 존재해, 어떤 조합을 골라도 빈 화면이 나오지 않는다. 10만/30만 안은 매크로 밴드에서 게임·교육 카테고리가 0명이 되어 탈락시켰다.

한편 마이크로 밴드가 129명(64%)으로 두껍다는 문제가 남는다. 이를 **구간을 더 쪼개서 풀지 않고 정렬·필터로 푸는 쪽을 선택**했다. 구간을 4~5단으로 늘리면 각 밴드의 후보가 얇아져 다시 "0명" 위험이 커지고, 사용자가 구간 정의를 학습해야 하는 부담도 생긴다. 후보가 20명 이상이면 화면에서 정렬 사용을 안내한다.

### 3.4 결측·예외 처리

**advertiser_rating 공란 27명 (13.5%)**

데이터를 확인하니 이 27명은 **전원 `total_campaign_count`가 0**이었다. 즉 "평점이 누락된 것"이 아니라 **광고 집행 이력이 없는 신인**이다. 그리고 이들은 팔로워 중앙값 39,219로 전체 중앙값(14,918)의 2.6배, 평균 조회수도 약 2배로 **채널 규모는 오히려 큰 편**이다.

처리 방식을 정하기 전에 **두 안을 시뮬레이션했다.**

| 처리 | 27명의 순위 (최고 / 중앙 / 최저) | TOP50 진입 |
|---|---|---|
| 0점 처리 | 132 / 184 / 200위 | 0명 |
| 중립 처리(평점=중앙값 4.5) | 65 / 152 / 200위 | 0명 |

**중립 처리로도 한 명도 상위에 오르지 못했다.** 평점을 보정해줘도 집행 경험 축에서 전원 0점을 맞기 때문에 구조적으로 올라올 수 없었다. 즉 이 문제는 **가중치 조정으로 풀리지 않는 문제**였고, 그래서 노출 구조를 바꾸는 쪽으로 방향을 틀었다.

최종 처리:
- 평점은 **중앙값으로 중립 대체**하되(감점하지 않음),
- 결과를 **"추천 크리에이터(검증)" / "미검증 후보"** 두 섹션으로 분리 노출하고,
- 각 카드에 `캠페인 이력 없음 · 미검증` 태그를 붙여 **추정과 사실을 구분**한다.

**avg_campaign_budget_krw = 0원 (27명, 위와 동일 인원)**

과제가 명시한 결측(공란) 외에 **0원이라는 숨은 예외**가 있었다. 이를 그대로 두면 "단가 0원 ≤ 모든 예산"이 되어 **모든 예산 조건을 무조건 통과**해버린다. 따라서 0원은 결측과 동일하게 취급하고, **카테고리별 단가 중앙값으로 추정**해 필터에 반영한다. 추정치임은 카드에 `단가 미확인 · 카테고리 평균 추정` 태그로 명시한다.

**후보 0명**

빈 화면 대신 **실제로 계산한 완화안**을 제시한다. 예: "예산을 570만원으로 상향 — 이 조건의 최저 단가 5,670,000원 기준", "팔로워 규모를 마이크로로 변경 — 12명 추천 가능". 각 제안에는 **몇 명이 나오는지**를 함께 표시해 사용자가 결과를 예측하고 선택할 수 있게 했다.

### 3.5 예산 입력의 해석

"예산: 숫자 입력"만 있고 1인 단가인지 캠페인 총예산인지 명시돼 있지 않다. **총예산으로 해석**했다. 근거는 ①광고주가 기획 단계에서 갖고 있는 숫자는 "이번 캠페인 예산"이지 "1인당 단가"가 아니고, ②실제 인플루언서 캠페인은 단일 집행보다 여러 명 믹스가 일반적이기 때문이다.

단, 단가가 예산보다 훨씬 낮다고 해서 감점하지 않는다. 남는 예산은 **조합 여력**이지 결함이 아니기 때문이다.

### 3.6 예산 효율 — 도달 1인당 비용(CPR)

```
CPR = 단가 ÷ 실질 도달
```

데이터를 계산해보니 이 값의 편차가 매우 컸다.

```
중앙값 2,286원 · 최저 111원 · 최고 55,089원  →  약 498배 차이
```

**같은 돈으로 살 수 있는 도달이 최대 498배 차이난다**는 뜻이다. 광고주 입장에서 이보다 직접적인 의사결정 정보는 없는데, 스코어 3축(도달·평점·경험) 어디에도 이 정보가 들어 있지 않았다. 실제로 확인해보니 **추천 점수 TOP10과 효율 TOP10은 절반(5명)만 겹쳤다.** 즉 효율은 기존 점수로 대체되지 않는 별개의 정보다.

카테고리별로도 구조적 차이가 있었다.

| 카테고리 | CPR 중앙값 |
|---|---|
| 게임 | 1,217원 |
| 교육 · 여행 | 2,084원 |
| 패션 | 2,580원 |
| 라이프스타일 | 2,978원 |
| 뷰티 | 2,996원 |

게임과 뷰티가 2.5배 차이난다. 같은 예산이라도 카테고리에 따라 기대할 수 있는 도달량이 다르다는 뜻이라, 광고주가 예산을 세울 때 알아야 할 정보다.

**그런데 이 지표를 스코어 가중치에는 넣지 않았다.** 효율만 좇으면 단가가 싼 나노 채널이 항상 상위를 차지하는데, 도달 절대량이 필요한 캠페인에서는 이것이 오답이 된다. 효율이 좋은 것과 캠페인 목표를 달성하는 것은 다른 문제다. 그래서 **카드에 값을 표시하고 정렬 축으로 제공하되, 판단은 사용자에게 남기는** 방식을 택했다. 효율 상위 25%(1,140원 이하)에는 태그를 붙여 눈에 띄게 했다.

### 3.7 도달률 위험 신호

`평균 조회수 ÷ 팔로워` 는 "팔로워 중 실제로 콘텐츠를 보는 비율"이다. 이 값이 비정상적으로 낮으면 팔로워 수가 실제 도달로 이어지지 않는다는 신호가 된다.

기준선은 플랫폼마다 달랐다.

| 플랫폼 | 중앙값 | 하위 10% |
|---|---|---|
| 유튜브 | 20.9% | 9.5% |
| 인스타그램 | 33.1% | 19.2% |

**두 플랫폼의 기준선이 1.6배 차이나므로 고정 임계값(예: 10% 미만)을 쓰면 안 된다.** 10%를 기준으로 잡으면 유튜브만 12명 걸리고 인스타그램은 한 명도 걸리지 않는다 — 인스타그램의 하위권이 놓쳐진다. 그래서 **같은 플랫폼 안에서 하위 10%인 경우**에만 태그를 붙였다(19명).

태그는 경고가 아니라 **확인 요청**으로 문구를 정했다("팔로워 대비 조회수 18% · 인스타그램 하위 10% — 확인 필요"). 낮은 도달률은 유령 팔로워일 수도, 최근 알고리즘 변화일 수도, 콘텐츠 형식 차이일 수도 있어 단정할 근거가 없기 때문이다. 판단 재료만 주고 결론은 사용자에게 남긴다.

---

## 4. 제외한 기능과 그 이유

| 제외한 것 | 이유 |
|---|---|
| **팔로워 수 자체의 가중치** | 규모는 이미 사용자가 입력으로 선택한 조건이다. 선택한 밴드 안에서 다시 규모로 점수를 주면 사용자의 선택을 뒤집는 셈이 된다. |
| **구간 세분화(4~5단)** | 3.3에서 서술. 밴드가 얇아져 0명 위험이 커지고, 정렬·필터로 대체 가능하다고 판단했다. |
| **참여율 단독 가중치** | 데이터상 변별력이 거의 없어(전 구간 1.4%p 내) 노이즈에 휘둘린다. 도달 계산에 곱셈으로 반영하는 편이 의미 있다. 단 정렬 옵션으로는 남겨 사용자가 직접 볼 수 있게 했다. |
| **신인 자동 제외** | 광고 이력이 없다는 것이 채널의 품질이 나쁘다는 뜻은 아니다. 감추는 대신 분리해 보여주고 판단은 사용자에게 남긴다. |
| **프레임워크(React 등)** | 평가 대상이 추천 로직과 문서이지 빌드 환경이 아니다. 단일 HTML로 만들어 검토자가 즉시 실행할 수 있게 했다. |
| **로그인·저장·발주 등 후속 플로우** | 과제 범위(입력→추천) 밖이며, 프로토타입의 검증 질문과 무관하다. |
| **단가 자체의 가중치** | 아래에 별도 서술. |
| **CPR(예산 효율)의 스코어 가중치 편입** | 3.6에서 서술. 효율만 좇으면 나노 채널이 항상 1위가 되어 도달 절대량이 필요한 캠페인에서 오답이 된다. 표시·정렬 축으로만 제공한다. |

### 단가를 점수 축에서 뺀 이유

단가는 데이터에서 가장 다루기 쉬운 숫자라 스코어에 넣고 싶은 유혹이 있다. 넣지 않기로 한 근거는 셋이다.

**① 넣어도 얻는 것이 없다.** 단가를 4번째 축으로 추가해 순위 변화를 측정했다(낮을수록 가점).

| 단가 반영 비중 | TOP10 겹침 | TOP10 평균 실질 도달 |
|---|---|---|
| 10% | 9/10 | 1,637 (+5%) |
| 20% | 7/10 | 1,440 (**-7%**) |
| 30% | 6/10 | 1,498 (-3%) |

순위는 흔들리는데 대가로 도달이 떨어진다. 싼 채널을 위로 올린 값이다.

**② 단가는 크리에이터의 품질이 아니라 광고주의 제약이다.** "단가가 낮아서 점수가 높다"는 예산 1억을 쓰는 광고주에게 아무 의미가 없다. 반대로 비싸다고 감점하면, 그 광고주가 원하는 큰 채널을 오히려 밀어낸다. 예산 상황은 사람마다 다르므로 **점수(모두에게 동일한 평가)가 아니라 필터(각자의 조건)로 처리하는 것이 맞다.**

**③ 이미 세 곳에서 쓰이고 있다.** 필터(`단가 ≤ 예산`), 효율 지표(CPR = 단가 ÷ 도달), 조합 제안(예산 배분). 점수 축에 또 넣으면 같은 정보를 네 번 쓰는 셈이다. 게다가 단가는 팔로워와 상관 0.74로 **규모의 함수에 가까워**, 도달 축에 이미 반영된 정보를 중복 계산하게 된다.

**남는 약점.** 예산 효율은 광고주에게 실제로 중요한 정보인데, 현재는 **정렬을 눌러야만 드러난다.** 기본 화면의 첫인상에서는 보이지 않는다. 상위 25%에 태그를 붙여 완화했지만 완전한 해결은 아니다. 실서비스라면 캠페인 목표(도달 확대형 / 효율형)를 사용자가 먼저 고르게 하고 그에 따라 기본 정렬을 바꾸는 방향이 맞다고 본다.

### 조합 제안에 대하여 (별도 탭)

조합 제안은 과제 요구 항목이 아니다. 그러나 실제 캠페인이 단일 집행보다 믹스로 이루어진다는 점에서 실무 가치가 크다고 판단해, **메인 흐름을 덮어쓰지 않는 별도 탭**으로 넣었다.

처음에는 기본 OFF인 체크박스로 만들어 단독 결과 아래에 붙였는데, 두 결과가 한 화면에 섞이니 "지금 보는 것이 1인 추천인지 조합인지" 구분이 되지 않았다. **옵션을 켜고 끄는 문제가 아니라 두 결과가 같은 지면에 있으면 안 되는 문제**여서, `단독 1인` / `조합 제안` 탭으로 분리했다. 기본값은 단독이므로 과제가 요구한 흐름은 그대로 유지된다.

특히 조합 모드의 **"테스트형"** 구성은 3.4의 문제에 대한 또 하나의 답이다. 단일 랭킹에서는 신인이 어떤 가중치로도 상위에 오를 수 없지만, **"검증된 크리에이터로 성과를 담보하고 남는 예산으로 신규 채널 1명을 테스트한다"** 는 구성에서는 신인이 **역할로서** 등장할 수 있다. 광고주가 실제로 하는 리스크 분산 방식을 그대로 옮긴 것이다.

---

## 5. 데이터에 없어 반영하지 못한 지표

실제 매칭에서는 아래 지표들이 중요하게 작동하지만, 제공된 데이터에 존재하지 않아 이번 스코어에는 반영하지 못했다. 실서비스라면 우선 확보를 제안할 항목들이다.

- **팬 연령·성별 분포** — 카테고리 일치보다 타깃 일치가 실제 전환에 가깝다.
- **플랫폼별 주 사용 연령층** — 같은 카테고리라도 플랫폼에 따라 도달하는 층이 다르다.
- **팬 충성도** — 댓글·재시청·커뮤니티 반응 등. 참여율만으로는 일회성 반응과 구분되지 않는다.
- **크리에이터 활동 빈도** — 업로드 주기가 무너진 채널은 과거 지표가 현재를 대변하지 못한다.
- **광고 노출 빈도** — 광고가 과도한 채널은 팬 피로도가 높다. 이번 데이터로는 `총 집행 건수` 대비 활동 기간을 알 수 없어 근사조차 어려웠다. 참고로 집행 25건 이상인 31명의 평균 평점은 4.39로 전체 평균 4.42와 거의 차이가 없어, **집행 건수만으로는 피로도를 판정할 수 없다**는 것만 확인했다.
- **팔로워 추세(성장/정체/하락)** — 현재 팔로워 수보다 방향이 중요할 때가 많다. 단일 시점 스냅샷만 제공되어 계산할 수 없었다.

이 중 **팔로워 추세**와 **활동 빈도**는 시계열만 확보되면 즉시 스코어에 편입 가능하다고 본다.

---

## 6. 알려진 한계

- 스코어의 가중치(45/30/25)는 실무 판단에 근거한 초기값이며, 실제 캠페인 성과와 대조해 검증된 값이 아니다. 다만 민감도 분석으로 **±5 범위에서는 결과가 바뀌지 않음**을 확인했다(3.2). 정밀한 값을 특정할 수는 없지만, 이 데이터에서는 정밀할 필요가 없다는 것까지는 말할 수 있다.
- 카테고리 중앙값 단가 추정은 신인의 실제 협상가와 다를 수 있다. 추정임을 UI에 명시하는 것으로 대응했다.
- 조합 제안은 단가가 낮은 후보부터 채우는 그리디 방식이라 최적해가 아니다. 예산 소진율과 도달 합계를 함께 표시해 사용자가 판단하도록 했다.
- CPR과 도달률 태그는 **단일 시점 스냅샷** 기준이다. 최근 업로드가 없는 채널의 낮은 도달률은 채널 품질이 아니라 활동 중단의 결과일 수 있으나, 시계열이 없어 구분하지 못한다.

### 전수 검증

로직 변경 후 **예산 11종 × 팔로워 3종 × 플랫폼 3종 × 카테고리 13조합 = 1,287 조건**에 대해, 각각 **인원 지정 5종 × 대안 조합 3회**를 돌려 **19,305회**를 자동 검증했다.

| 검증 항목 | 결과 |
|---|---|
| 점수 범위 이탈 · NaN | 0건 |
| 예산 초과 조합 | 0건 |
| 지정 인원 초과 · 중복 포함 | 0건 |
| 채울 수 있는데 못 채운 조합 | 0건 |
| 테스트형에 신인 2명 이상 배정 | 0건 |

초기 검증에서는 **"채울 수 있는데 못 채운 조합"** 항목에서 19건이 걸렸다. "다른 조합 보기"의 후보 회전 로직이 저단가 재구성 단계에도 적용되어, 예산으로 채울 수 있는데도 인원을 못 채우는 경우가 있었다. 회전 결과가 목표 인원에 미달하면 회전 없이 최저 단가로 다시 구성하는 단계를 추가해 해소했다.

한편 **후보 0명이면서 완화안도 제시하지 못하는 조건이 21건(1.6%)** 남아 있다. 예산이 극단적으로 낮아(5만원) 어떤 조건 완화로도 후보가 생기지 않는 경우로, 현재는 안내 문구만 노출된다.

### 예산대별 커버리지

"후보 0명"이 얼마나 자주 발생하는지는 예산에 거의 전적으로 좌우된다.

| 예산 | 후보 0명 비율 |
|---|---|
| 5만 · 10만원 | 100% |
| 30만원 | 82% |
| 50만원 | 70% |
| 100만원 | 38% |
| 300만원 | 21% |
| 500만원 | 13% |
| 1,000만원 이상 | 8% |

전체 조건을 평균하면 45%지만, 이는 **애초에 집행이 불가능한 예산**(5만·10만원)이 비율을 끌어올린 결과다. 실제 캠페인이 성립하는 50만원 이상 구간만 보면 **24%**, 그중 완화안조차 제시하지 못하는 경우는 **0.1%**로 떨어진다.

데이터의 최저 단가가 30만원대이므로 이보다 낮은 예산에서 후보가 없는 것은 로직의 결함이 아니다. 다만 사용자가 그 사실을 빈 화면으로 마주하지 않도록, **"이 조건의 최저 단가는 얼마이고, 예산을 얼마로 올리면 몇 명이 나오는지"** 를 계산해 제시한다.

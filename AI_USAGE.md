# AI 활용 기록

이용환 · 2026.08

---

## 1. 사용한 도구와 이유

| 도구 | 용도 |
|---|---|
| **Claude Opus 5 (Cowork)** | 과제 해석, 로직 설계 토론, 데이터 분석 스크립트 작성·실행, 코드 작성, 문서 초안 |

단일 도구로 진행했습니다. 이 과제는 "코드를 빨리 찍어내는 것"보다 **판단의 근거를 세우는 일**이 핵심이라, 코드 자동완성형 도구보다 **대화하며 가설을 검증할 수 있는 환경**이 맞다고 봤습니다.

Cowork를 고른 이유가 여기 있습니다. 이 환경은 대화 중에 **분석 스크립트를 즉석에서 작성해 실행하고 결과를 바로 볼 수 있습니다.** 덕분에 "이렇게 하면 될 것 같은데" → "그럼 계산해보자" → 숫자 확인 → 채택 또는 기각까지의 루프가 몇 분 단위로 돌았습니다. 아래 3번의 방향 전환들은 전부 이 루프에서 나왔고, **한 번에 답을 얻은 것은 하나도 없습니다.**

---

## 2. 컨텍스트로 제공한 것

- `dummy_creators.csv` 원본 (200행)
- 과제 안내문 전문 (요구사항·제약·평가 포인트)
- 제 실무 경험 — 크리에이터 플랫폼(Fantrie)에서 크리에이터 30명+ 영입·계약·정산을 담당한 경험, 커머스 셀러로 광고를 집행한 경험

마지막 항목이 중요했습니다. AI에게 "일반적으로 좋은 추천 로직"을 물으면 교과서적인 답(팔로워·참여율 가중 합산)이 나옵니다. **실무에서 실제로 무엇을 보고 크리에이터를 고르는지**를 제가 제공했을 때부터 로직이 달라지기 시작했습니다.

---

## 3. AI 결과물을 수정·재요청한 판단 사례

### 사례 1 — "신인을 중립 처리하면 공정하다"는 가정을 계산으로 뒤집음

**초기 방향(AI 제안 + 제 동의):** `advertiser_rating` 결측자는 평점을 중앙값으로 채워 불이익을 주지 않는다.

**의심한 지점:** 평점만 채워주면 정말 노출될까? 집행 경험 축은 여전히 0인데?

**검증:** 실제 데이터로 두 방식을 시뮬레이션했습니다.

```
0점 처리   → 27명 순위: 최고 132위 / 중앙 184위 · TOP50 진입 0명
중립 처리  → 27명 순위: 최고  65위 / 중앙 152위 · TOP50 진입 0명
```

**판단:** 중립 처리로도 한 명도 상위에 오르지 못했습니다. **가중치로는 풀 수 없는 문제**라고 결론 내리고, 방향을 "점수 조정"에서 **"노출 구조 분리"**로 바꿨습니다. 검증/미검증 섹션을 나누고 태그로 명시하는 현재 구조가 여기서 나왔습니다.

> 이 사례가 이번 과제에서 가장 큰 방향 전환이었습니다. AI가 제안한 처리 방식 자체는 틀리지 않았지만, **그 처리가 실제로 의도한 결과를 만드는지는 계산해봐야 알 수 있었습니다.**

### 사례 2 — 과제에 명시되지 않은 예외를 직접 발견

과제는 `advertiser_rating` 공란을 예고했습니다. 그런데 데이터를 프로파일링하다가 **`avg_campaign_budget_krw`에 0원인 행이 27건** 있는 것을 발견했습니다.

AI가 처음 작성한 필터는 `단가 ≤ 예산`이었고, 이대로면 **0원짜리는 어떤 예산 조건에도 무조건 통과**합니다. 예고된 함정만 처리하고 넘어갔다면 놓쳤을 부분입니다. 결측과 동일하게 취급하고 **카테고리 중앙값으로 추정**하도록 수정했습니다.

### 사례 3 — 참여율 가중치를 걷어냄

**초기 코드:** `조회수 30% + 참여율 20% + 평점 30% + 경험 20%`

**의심한 지점:** 참여율이 정말 변별력이 있나?

**검증:** 분포를 확인했습니다. `p25 6.47 / 중앙값 7.10 / p75 7.90` — **전 구간이 1.4%p 안에** 몰려 있었습니다.

**판단:** 이 상태로 20% 가중치를 주면 소수점 차이가 순위를 흔듭니다. 참여율을 독립 축에서 빼고 **`조회수 × 참여율 = 실질 도달`** 이라는 하나의 의미 있는 축으로 합쳤습니다. 대신 사용자가 직접 보고 싶을 수 있으니 **정렬 옵션으로는 남겼습니다.**

### 사례 4 — 팔로워 구간을 "정하지 말고 대보고 정하기"로

AI에게 구간을 물으면 업계 통념(나노 1만 / 마이크로 10만 / 매크로 100만+)을 제시합니다. 그런데 이 데이터는 **최대 팔로워가 498,652명**이라, 통념대로 하면 **매크로가 공집합**이 됩니다.

세 가지 안을 만들어 데이터에 대봤습니다.

```
1만/10만  → 44 / 129 / 27   · 모든 밴드×카테고리에 후보 1명 이상  ✅
5만/10만  → 122 / 51 / 27   · 5만 경계에 데이터적 근거 없음
10만/30만 → 173 / 14 / 13   · 매크로에 게임·교육 0명 발생        ❌
```

숫자를 보고 1안을 채택했습니다. **"AI에게 기준을 묻지 말고, 기준 후보를 만들어 데이터로 판정하게 하는 것"** 이 이번에 얻은 방법입니다.

> 다만 이 검증은 **플랫폼 필터를 도입하기 전**의 것이었고, 나중에 그 사실을 놓친 채 문서에 남겨뒀다가 제출 직전에 발견했습니다(사례 9).

### 사례 5 — 아무도 지적하지 않은 편향을 뒤늦게 발견

기능이 거의 완성된 뒤, 결과 화면을 보다가 **추천 상위권에 인스타그램 계정이 유난히 많다**는 인상을 받았습니다. 우연인지 구조적 문제인지 확인이 필요했습니다.

플랫폼별로 `조회수 ÷ 팔로워`를 계산해봤습니다.

```
유튜브     중앙값 20.9%
인스타그램  중앙값 33.1%    ← 1.6배
```

**같은 실력이어도 인스타그램이 높게 나오는 지표**였습니다. 이 상태로 두 플랫폼을 한 줄로 세워 백분위를 매기면, 인스타그램 계정이 **실력과 무관하게 상위를 차지**합니다. 점수는 정상 작동하고 있었지만 결과는 편향돼 있었습니다.

플랫폼별로 나눠 각 그룹 안에서 백분위를 매기도록 고쳤고, TOP10 구성이 유튜브 6 : 인스타그램 4로 균형을 찾았습니다.

> 이 사례가 불편했던 이유는, **아무도 이걸 버그라고 부르지 않았을 것**이기 때문입니다. 코드는 의도대로 돌고 있었고, 검증 스크립트도 통과했고, 화면도 정상이었습니다. 숫자를 의심하고 플랫폼별로 쪼개보지 않았다면 끝까지 몰랐을 겁니다. AI는 물어본 것만 계산해주지, **무엇을 의심해야 하는지는 알려주지 않습니다.**

### 사례 6 — 기능 욕심을 잘라낸 지점

조합 제안(예산을 나눠 여러 명 구성)은 실무 가치가 크다고 판단해 만들었지만, **과제 요구 항목이 아닙니다.** 메인 흐름을 덮어쓰지 않도록 처음에는 **기본 OFF인 체크박스 옵션**으로 넣었습니다.

그런데 써보니 문제가 있었습니다. 단독 추천 결과 **아래에** 조합 제안이 붙으니 두 결과가 뒤섞여 읽혔고, "지금 보고 있는 게 1명 추천인지 조합인지" 헷갈렸습니다. 그래서 **탭으로 분리**했습니다 — `단독 1인` / `조합 제안`. 기본은 단독이라 과제 요구 흐름이 그대로 유지되고, 조합은 원할 때만 들어갑니다.

**옵션을 켜고 끄는 문제가 아니라 두 결과가 같은 화면에 있으면 안 되는 문제**였습니다. 처음의 "기본 OFF" 판단은 방향은 맞았지만 해법이 틀렸습니다.

AI는 요청하면 기능을 계속 만들어줍니다. **무엇을 만들지보다 무엇을 넣지 않을지, 넣는다면 어디에 둘지 정하는 것**이 이 과제에서는 더 중요한 결정이었습니다.

### 사례 7 — 전수 시뮬레이션으로 버그를 찾음 (AI가 만든 코드의 검증)

기능이 다 붙은 뒤 "돌아가는 것 같다"에서 멈추지 않고 **모든 입력 조합을 자동으로 돌려보는 스크립트**를 AI에게 작성하게 했습니다. 예산 11종 × 팔로워 3종 × 플랫폼 3종 × 카테고리 13조합 = 1,287 조건, 각각 인원 지정 5종 × 대안 조합 3회 = **19,305회**.

여기서 **19건의 버그**가 나왔습니다. 예산으로 채울 수 있는데도 인원을 못 채우는 케이스였습니다.

```
100만원 / 나노 / 교육 / 3명 지정  →  실패
그러나 최저 단가 3명 합계는 93만원 (채울 수 있음)
```

원인은 "다른 조합 보기"의 후보 회전 로직이 저단가 재구성 단계까지 적용된 것이었습니다. **사람이 손으로 클릭해서는 절대 못 찾을 조건**입니다 — 특정 예산·특정 카테고리·특정 인원·2번째 대안에서만 나타납니다.

> 이 사례에서 얻은 것: **AI가 코드를 빨리 만들어주는 만큼, 검증도 AI에게 대량으로 시켜야 한다**는 점입니다. 검증 코드를 짜는 비용이 낮아졌으니 "동작 확인"의 기준을 눈으로 몇 번 눌러보는 수준에서 전수 검증으로 올릴 수 있었습니다.

### 사례 8 — "더 뽑을 게 없나"를 AI에게 시키되, 채택 여부는 직접 판단

기능이 완성된 뒤 **제공된 데이터에서 아직 안 쓴 신호가 있는지** 탐색 스크립트를 돌렸습니다. 상관계수, 파생 지표, 분포 이상치를 훑는 코드였습니다. 나온 결과 중 셋을 채택하고 하나는 기각했습니다.

**채택 — 도달 1인당 비용(CPR)**
`단가 ÷ 실질 도달`이 111원 ~ 55,089원으로 **498배** 차이났습니다. 광고주에게 가장 직접적인 정보인데 스코어 3축 어디에도 없었습니다. 확인해보니 **점수 TOP10과 효율 TOP10은 절반만 겹쳐** 별개의 정보임이 확인됐습니다.

**단, 스코어 가중치에 넣자는 제안은 기각했습니다.** 효율만 좇으면 단가가 싼 나노 채널이 항상 1위가 되는데, 도달 절대량이 필요한 캠페인에서는 오답입니다. 표시·정렬 축으로만 제공하고 판단은 사용자에게 남기는 쪽으로 정했습니다.

**채택 — 도달률 주의 태그, 단 임계값은 수정**
AI의 첫 제안은 "조회수/팔로워 10% 미만을 유령 팔로워로 태그"였습니다. 그런데 플랫폼별 중앙값을 보니 유튜브 20.9% · 인스타그램 33.1%로 **기준선이 1.6배 달랐습니다.** 고정 10%를 쓰면 유튜브만 12명 걸리고 **인스타그램은 한 명도 안 걸립니다.** 임계값을 **플랫폼별 하위 10%** 로 바꿔 19명이 잡히도록 수정했습니다.

문구도 "유령 팔로워 의심"에서 **"확인 필요"** 로 바꿨습니다. 낮은 도달률은 유령 팔로워일 수도, 알고리즘 변화일 수도, 콘텐츠 형식 차이일 수도 있어 단정할 근거가 없습니다. **데이터가 말할 수 있는 범위를 넘는 라벨은 붙이지 않기로** 했습니다.

**채택 — 상관계수로 가중치 근거 보강**
평점이 다른 지표와 전부 무상관(±0.07 이내)이었습니다. **평점은 대체 불가능한 독립 신호**라는 뜻이라, 30% 가중치의 근거가 "제 판단"에서 "데이터로 확인된 사실"로 올라갔습니다. 반대로 단가는 팔로워(0.74)·도달(0.71)과 강하게 붙고 **참여율과는 -0.51** — 비싼 채널일수록 반응 밀도는 낮다는 뜻이라, 단가를 점수 축에서 뺀 판단이 뒷받침됐습니다.

**기각 — 집행 건수를 광고 피로도 지표로 사용**
"집행 25건 이상은 광고 과다 노출"이라는 가설을 세웠는데, 계산해보니 **이들의 평균 평점 4.39 vs 전체 4.42** 로 유의미한 차이가 없었습니다. 근거가 없으니 넣지 않고, **"이 데이터로는 피로도를 판정할 수 없다"** 는 결론만 PRD에 남겼습니다.

> 이 사례의 핵심은 네 개 중 하나를 **버렸다**는 점입니다. AI가 찾아준 신호는 전부 "그럴듯"했지만, 실제로 숫자를 대보면 근거가 없는 것이 섞여 있었습니다.

### 사례 9 — 제출 직전 검증에서 **문서의 오류**를 발견

코드가 다 끝난 뒤, 마지막으로 예산 구간을 **10만원 단위로** 다시 훑었습니다. 1차 검증은 예산을 11종(5만·10만·30만·50만·100만…)만 봤는데, **간격이 넓으면 그 사이에 숨은 문제를 못 잡기** 때문입니다.

여기서 검사 항목을 하나 새로 추가했습니다 — **예산 역전**. *예산을 10만원 올렸는데 후보 수나 구성 인원이 오히려 줄어드는* 경우입니다. 사용자 입장에서 가장 납득하기 어려운 오작동인데, 예산을 띄엄띄엄 보면 절대 드러나지 않습니다.

```
조건 19,404건 · 조합 계산 291,060회
로직 오류 0건 · 예산 역전 0건
```

**코드는 깨끗했는데, 대신 문서가 틀린 것이 나왔습니다.**

PRD에 이렇게 적어뒀었습니다.

> 세 밴드 모두에서 10개 카테고리 각각 최소 1명 이상이 존재해, 어떤 조합을 골라도 빈 화면이 나오지 않는다.

이 검증은 **플랫폼 필터를 도입하기 전에** 한 것이었습니다. 플랫폼까지 포함해 다시 세니 **60개 조합 중 8개가 0명**이었습니다. 기능을 추가하면서 **예전 검증 결과가 조용히 무효가 된 것**을 몰랐던 겁니다.

200명으로 60칸을 채우면 칸당 평균 3.3명이라 빈 칸이 생기는 게 당연하고, 구간 정의의 결함은 아닙니다. 하지만 **"빈 화면이 나오지 않는다"는 문장은 사실이 아니었으므로** PRD를 고쳤습니다.

> 이 사례에서 얻은 것: **문서도 코드처럼 낡는다**는 점입니다. 기능을 추가할 때 코드는 테스트가 잡아주지만, 문서에 적힌 과거의 검증 결과는 아무도 다시 확인하지 않습니다. 마지막에 "문서의 주장이 지금도 참인가"를 데이터로 대조하는 절차가 필요했습니다.

### 사례 10 — UI 되돌리기

예산 입력칸에 콤마 자동 삽입(`5,000,000`)을 구현했다가, 실제로 써보니 커서 위치가 튀고 입력이 어색해서 **되돌렸습니다.** 대신 입력칸 아래 한글 환산(`500만원`)과 빠른 선택 칩만 남겼습니다. 구현 가능한 것과 쓸 만한 것은 다르다고 판단한 경우입니다.

---

## 4. 작업 방식

- **분석 → 판단 → 구현** 순서를 지켰습니다. 코드를 먼저 쓰지 않고, 데이터 프로파일링(분포·결측·이상치)을 먼저 돌린 뒤 그 숫자를 보고 로직을 정했습니다.
- **가설은 계산으로 확인**했습니다. "이렇게 하면 될 것 같다"를 그대로 두지 않고 시뮬레이션 스크립트로 결과를 확인한 뒤 채택/기각했습니다(사례 1·3·4·8).
- **완성 후 전수 검증**했습니다. 눈으로 몇 번 눌러보는 대신 **총 310,365회**(전 구간 19,305회 + 실사용 구간 10만원 단위 291,060회)를 돌렸습니다. 1차에서 조합 버그 19건, 제출 직전 재검증에서 문서의 사실 오류를 잡았습니다(사례 7·9).
- **커밋은 스쿼시하지 않았습니다.** 아래 순서가 히스토리에 그대로 남아 있고, 각 커밋 메시지에 그때의 판단 근거와 숫자를 적었습니다.

```
1  데이터 프로파일링 — 분포·결측·이상치 확인
2  신인 처리 시뮬레이션 — 중립 처리 가정을 기각
3  1차 전수 시뮬레이션 — 인원 미충족·예산 초과 탐지
4  프로토타입 구현 — 입력 → 필터 → 스코어 → 추천 결과
5  전수 검증 19,305회 — 조합 버그 수정 + 미사용 신호 반영
6  문서화 — README · PRD · flowchart · AI_USAGE
7  최종 검토 — 10만원 단위 재검증(291,060회) + 문서 오류 수정
```

**코드보다 분석이 먼저(1·2·3)** 오고, 구현 뒤에 **검증이 두 번(5·7)** 붙은 순서입니다. 마지막 7번은 제출 직전에 예산 구간을 10만원 단위로 다시 훑은 것으로, 여기서 코드가 아니라 **문서의 사실 오류**가 나왔습니다(사례 9).

커밋을 더 잘게 쪼갤 수도 있었지만 그렇게 하지 않았습니다. `index.html`이 단일 파일이라 "스코어 로직 → 예외 처리 → 플랫폼 분리"로 나누려면 **실제로는 존재한 적 없는 중간 상태를 만들어내야** 했기 때문입니다. 커밋 수를 늘리자고 히스토리를 지어내는 것은 "작업 과정을 그대로 남겨달라"는 요구에 오히려 어긋난다고 판단해, 파일이 실제로 만들어진 순서 그대로 6개만 남겼습니다. 대신 각 커밋 메시지에 그 시점의 판단 근거와 숫자를 적었습니다.

---

## 5. AI가 하지 않은 것

- 가중치 배분(45/30/25)의 최종 결정 — 도달을 최우선에 둔 것은 광고 성과에 가장 직접적인 축이라는 제 판단입니다. 다만 이 값이 최적이라고 주장하지 않기로 한 것도 제 결정입니다. AI에게 "최적 가중치를 찾아달라"고 하면 그럴듯한 근거와 함께 숫자를 만들어 줍니다. 대신 **"이 값이 결과를 얼마나 좌우하는가"를 측정**해, ±5 범위에서는 순위가 바뀌지 않고 축을 빼면 무너진다는 사실만 문서에 남겼습니다.
- 플랫폼 편향을 의심한 것 — AI는 물어본 것만 계산합니다. 결과 화면을 보고 "인스타가 많은데 우연인가"를 의심한 것은 제 쪽이었고, 그 의심이 없었다면 지금도 편향된 채로 돌고 있었을 겁니다(사례 5).
- "신인을 어떻게 다룰지"의 방향 전환 — 계산 결과를 보고 구조를 바꾸기로 한 결정.
- 예산을 총예산으로 해석한 것 — 실제 캠페인이 믹스로 집행된다는 경험에서 나온 정의입니다.
- 조합 모드에 "테스트형(검증+신인)"을 넣은 것 — 광고주가 실제로 하는 리스크 분산 방식을 옮긴 것입니다.
- CPR을 스코어에 넣지 않기로 한 것 — AI는 좋은 지표를 찾으면 점수에 편입하자고 제안합니다. 넣지 않는 편이 맞다고 판단한 근거는 "효율 1위가 캠페인 목표 달성 1위는 아니다"라는 실무 감각입니다.
- 도달률 태그 문구를 "유령 팔로워 의심"에서 "확인 필요"로 낮춘 것 — 데이터가 말할 수 있는 범위를 넘지 않기 위한 결정입니다.

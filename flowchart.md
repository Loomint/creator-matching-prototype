# 로직 흐름

입력부터 추천 결과 출력까지의 전체 흐름입니다. 조건 분기와 예외 처리를 포함합니다.

---

## 1. 전체 흐름

```mermaid
flowchart TD
    A([시작]) --> B[CSV 로드]
    B --> B1{로드 성공?}
    B1 -- 실패 --> BX[안내: 로컬 서버로 실행 필요<br/>python3 -m http.server 8000]
    B1 -- 성공 --> C[안전 파싱<br/>숫자 변환 · 결측 null 처리]
    C --> C1[카테고리별 단가 중앙값 계산<br/>→ 단가 미확인 추정용]
    C1 --> D[입력 대기]

    D --> E[/"① 총예산 · ② 플랫폼 · ③ 팔로워 규모 · ④ 카테고리(다중)"/]
    E --> F[1차 필터<br/>팔로워 구간 · 플랫폼 · 카테고리]
    F --> G[2차 필터<br/>단가 ≤ 총예산]

    G --> H{후보 수}
    H -- 0명 --> I[조건 완화안 계산]
    I --> I1[["· 다른 팔로워 규모 → n명<br/>· 예산 상향 → 최저 단가 기준<br/>· 카테고리 해제 → n명"]]
    I1 --> I2[/완화안 카드 노출<br/>클릭 시 조건 반영 후 재실행/]
    I2 --> D

    H -- 1명 이상 --> J[스코어링]
    J --> K[검증 / 미검증 분리]
    K --> L{결과 화면 탭}
    L -- "단독 1인 (기본)" --> M[/추천 결과 렌더/]
    L -- 조합 제안 --> N[조합 계산<br/>안정형 · 테스트형]
    N --> M
    M --> O[/정렬 변경 · 인원 지정 · 다른 조합 보기/] --> M
```

입력은 **네 항목을 순서대로** 받습니다. 예산을 가장 먼저 두는 이유는, 광고주가 기획 단계에서 이미 손에 쥐고 있는 유일한 확정 숫자이기 때문입니다. 나머지 셋은 예산에 따라 조정할 수 있는 조건입니다.

**추천 방식(단독 / 조합)은 입력이 아니라 결과 화면의 탭**입니다. 입력 단계에서 미리 고르게 하면 사용자가 결과를 보기 전에 판단해야 하는데, "1명으로 갈지 여러 명으로 나눌지"는 후보를 본 뒤에 정하는 것이 자연스럽다고 봤습니다.

---

## 2. 데이터 안전 처리

원본 CSV는 수정하지 않고, 로드 시점에 예외를 흡수합니다.

```mermaid
flowchart TD
    A[CSV 한 행] --> B{advertiser_rating}
    B -- 공란 --> B1[rating = null<br/>verified = false]
    B -- 값 있음 --> B2[rating = 값<br/>verified = true]

    A --> C{avg_campaign_budget_krw}
    C -- 0원 또는 공란 --> C1[unitBudget = null<br/>→ 카테고리 중앙값으로 추정<br/>· 카드에 '추정' 태그]
    C -- 값 있음 --> C2[unitBudget = 값]

    A --> D{숫자 필드<br/>followers · view · er 등}
    D -- 파싱 실패 --> D1[0으로 대체<br/>NaN 전파 차단]
    D -- 정상 --> D2[숫자 사용]
```

**0원을 결측과 동일하게 처리하는 이유**
`0원 ≤ 모든 예산`이 성립해, 처리하지 않으면 어떤 예산 조건에도 무조건 통과하게 됩니다.

---

## 3. 스코어 계산

```mermaid
flowchart LR
    A[선택 구간 내 후보 집합] --> A2[플랫폼별로 분리<br/>유튜브 / 인스타그램]
    A2 --> B[각 그룹 내부 백분위 산출]

    B --> C1["실질 도달<br/>평균 조회수 × 참여율"]
    B --> C2["광고주 평점<br/>결측 시 중앙값 대체"]
    B --> C3["집행 경험<br/>총 캠페인 건수"]

    C1 -- "×0.45" --> D[가중 합산]
    C2 -- "×0.30" --> D
    C3 -- "×0.25" --> D
    D --> E([score 0~100])

    A --> F["별도 계산 · 점수 미반영"]
    F --> F1["CPR = 단가 ÷ 실질 도달<br/>→ 카드 표시 · 정렬 축"]
    F --> F2["조회수 ÷ 팔로워<br/>→ 플랫폼 하위 10%면 주의 태그"]
```

지표를 **선택된 팔로워 구간 안에서, 다시 플랫폼별로 나눠** 백분위로 환산합니다.

- **구간 분리** — 규모 편향 상쇄. 매크로를 골랐으면 매크로끼리 비교됩니다.
- **플랫폼 분리** — 조회수/팔로워 중앙값이 유튜브 20.9% · 인스타그램 33.1%로 1.6배 차이나, 섞으면 인스타그램이 구조적으로 유리해집니다.

**CPR과 도달률은 점수에 넣지 않고 별도 표시합니다.** CPR을 가중치에 넣으면 단가가 싼 나노 채널이 항상 상위를 차지해, 도달 절대량이 필요한 캠페인에서 오답이 되기 때문입니다.

---

## 4. 결과 노출 분기

```mermaid
flowchart TD
    A[스코어링 완료] --> B{캠페인 이력}
    B -- 있음 --> C[추천 크리에이터 섹션<br/>score 내림차순]
    B -- 없음 --> D[미검증 후보 섹션<br/>'캠페인 이력 없음' 태그]

    C --> E{후보 20명 이상?}
    D --> E
    E -- 예 --> F[정렬 사용 안내 노출]
    E -- 아니오 --> G[기본 노출]

    F --> H[카드 렌더]
    G --> H
    H --> H1[["점수 · 도달/신뢰/경험 3축 막대<br/>실제 값: 조회수 · 실질 도달 · 단가 · CPR"]]
    H --> H2[["근거 태그<br/>· 조건 일치: 카테고리 · 예산 소진율<br/>· 성과: 도달 상위 10/25% · 평점 · 효율 상위 25%<br/>· 주의: 미검증 · 단가 추정 · 도달률 하위 10%"]]
```

**분리 노출의 근거**
평점을 중립값으로 대체해도 신인 27명은 TOP50에 한 명도 진입하지 못했습니다(집행 경험 축에서 전원 0점). 단일 랭킹으로는 구조적으로 노출되지 않아, 섹션을 분리하고 판단 근거를 태그로 제공하는 방식을 선택했습니다.

---

## 5. 조합 제안 (별도 탭)

```mermaid
flowchart TD
    A["'조합 제안' 탭 선택"] --> B[안정형]
    A --> C[테스트형]

    B --> B1[1차: 점수 높은 순으로 예산 채움]
    B1 --> B1a{인원 목표 미달?}
    B1a -- 아니오 --> B2([구성 · 소진율 · 도달 합계])
    B1a -- 예 --> B1b[2차: 단가 낮은 순으로 재구성]
    B1b --> B1c{"다른 조합 보기(회전) 때문에<br/>여전히 미달?"}
    B1c -- 예 --> B1d[3차: 회전 없이 최저 단가로 재구성]
    B1c -- 아니오 --> B2
    B1d --> B2

    C --> C1{미검증 후보 존재?}
    C1 -- 없음 --> C2[테스트형 미노출]
    C1 -- 있음 --> C3[최상위 신인 1명 우선 배정]
    C3 --> C4[남은 예산을<br/>검증 후보로 채움]
    C4 --> C5([구성 · 소진율 · 도달 합계])
```

**테스트형의 의미**
검증된 후보로 성과를 담보하고 남는 예산으로 신규 채널 1명을 시험하는 구성입니다. 단일 랭킹에서 노출되지 못하는 신인이 **역할로서** 등장하는 경로이기도 합니다.
